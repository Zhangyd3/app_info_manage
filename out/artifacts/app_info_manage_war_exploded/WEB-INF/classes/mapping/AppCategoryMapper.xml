<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyd.appinfo.mapper.AppCategoryMapper">
  <cache></cache>
  <select id="findByParentId" resultType="com.zyd.appinfo.pojo.AppCategory">
    select id, categoryCode, categoryName, parentId, createdBy, creationTime, modifyBy, modifyDate from app_category where parentId=#{id}
  </select>
    <select id="findByLevel" resultType="com.zyd.appinfo.pojo.AppCategory">
      select id, categoryCode, categoryName, parentId, createdBy, creationTime, modifyBy, modifyDate from app_category where level=#{level}
    </select>
</mapper>